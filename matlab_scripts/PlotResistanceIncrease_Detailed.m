%% PlotResistanceIncrease_Detailed.m
% Summary: Generates detailed resistance-increase comparison figures for the
%          ageing dataset. The plots group cells by depth of discharge,
%          temperature, C-rate, average SoC, voltage window, storage profile,
%          and calendar-ageing condition.
%
% Usage: Set DataRoot if needed, then run this script with no arguments. The
%        script reads overview resistance tables from the cyclic and calendar
%        ageing data folders.
%
% Outputs: Resistance comparison PNG and PDF figures in this script's R-022
%          output directory.
%
% Authors: Tim Meulenbreuks, Róbinson Medina, NEXTBMS Team.
% Dependency files: Functions/getFigureOutputDir.m,
%                   Functions/plotResistanceIncreaseTile.m.
% Last documented: 2026-09-01

clear; close all; clc;

%% Set input/output folder
% Single configurable dataset root. Read-only (R-001).
% The combined manuscript PDFs are generated by PlotAgeingCombinedOverview.m;
% this script remains useful for detailed debug and exploration plots.
DataRoot = '\\tsn.tno.nl\RA-Data\SV\sv-072952\BTS Data\NEXTBMS\ZenodoRoot';
io_folder_cyclic   = fullfile(DataRoot, '4_Ageing', 'Cyclic_ageing_data');
io_folder_calendar = fullfile(DataRoot, '4_Ageing', 'Calendar_ageing_data');
% Primary publication output location (R-022).
scriptDir = fileparts(mfilename('fullpath'));
functionsDir = fullfile(scriptDir, '..', '..', 'Functions');
if ~exist(functionsDir, 'dir')
    functionsDir = fullfile(scriptDir, '..', 'Functions');
end
if exist(functionsDir, 'dir')
    addpath(functionsDir);
else
    error('Shared Functions folder not found from %s.', scriptDir);
end
pngsDir = getFigureOutputDir('PlotResistanceIncrease_Detailed');

input_csv_cyclic = fullfile(io_folder_cyclic, 'OverviewResistanceData_36cell.csv');
input_csv_calendar = fullfile(io_folder_calendar, 'OverviewResistanceData_5cell.csv');

fprintf('--- PlotResistanceIncrease_Detailed: starting figure generation ---\n');

%% Load and preprocess CYCLIC ageing data
combined_df_cyclic = readtable(input_csv_cyclic, 'VariableNamingRule', 'preserve');
combined_df_cyclic = renameColumnIfExists(combined_df_cyclic, 'CellNum', 'cell_number');
combined_df_cyclic = renameColumnIfExists(combined_df_cyclic, 'CheckupResistanceTimeStamp', 'Timestamp');
combined_df_cyclic = renameColumnIfExists(combined_df_cyclic, 'CheckupResistance_Ohm', 'Resistance [Ohm]');

combined_df_cyclic.("cell_number") = regexp(string(combined_df_cyclic.("cell_number")), 'Cell_\d+', 'match', 'once');
cell_list = unique(combined_df_cyclic.("cell_number"));
combined_df_cyclic.("Timestamp") = datetime(combined_df_cyclic.("Timestamp"), 'InputFormat', 'dd-MMM-yyyy HH:mm:ss');

%% Load and preprocess CALENDAR ageing data
combined_df_calendar = readtable(input_csv_calendar, 'VariableNamingRule', 'preserve');
combined_df_calendar = renameColumnIfExists(combined_df_calendar, 'CellNum', 'cell_number');
combined_df_calendar = renameColumnIfExists(combined_df_calendar, 'CheckupResistanceTimeStamp', 'Timestamp');
combined_df_calendar = renameColumnIfExists(combined_df_calendar, 'CheckupResistance_Ohm', 'Resistance [Ohm]');

combined_df_calendar.("cell_number") = regexp(string(combined_df_calendar.("cell_number")), 'Cell_\d+', 'match', 'once');
cell_list_calendar = unique(combined_df_calendar.("cell_number"));
combined_df_calendar.("Timestamp") = datetime(combined_df_calendar.("Timestamp"), 'InputFormat', 'dd-MMM-yyyy HH:mm:ss');

%% Plot 1: All cells overview - skipped (superseded by the comparison plots below).
fprintf('Skipping Plot 1 (All Cells Overview).\n');

%% Plot 2+3: Effect of DoD (col 1) and Effect of Temperature (col 2), combined 2x2 tiles.
fprintf('Generating Plot 2+3: Effect of DoD + Effect of Temperature (combined)...\n');
cell_plot_list_dod = {"Cell_12", "Cell_56", "Cell_89", "Cell_93", "Cell_27", "Cell_30", "Cell_16"};
label_list_dod = {"Cell_12 - 100% DoD", "Cell_56 - 100% DoD", "Cell_89 - 100% DoD", "Cell_93 - 100% DoD", "Cell_27 - 70% DoD", "Cell_30 - 40% DoD", "Cell_16 - 10% DoD"};
colormap_list_dod = {'b', 'r', 'g', [1 0.5 0], [0.5 0 0.5], [0.6 0.3 0], [1 0.75 0.8]};
Marker_list_dod = repmat({'-'}, 1, length(cell_plot_list_dod));

cell_plot_list_temp = {"Cell_60", "Cell_12", "Cell_56", "Cell_89", "Cell_93", "Cell_29"};
label_list_temp = {"Cell_60 - 0°C", "Cell_12 - 25°C", "Cell_56 - 25°C", "Cell_89 - 25°C", "Cell_93 - 25°C", "Cell_29 - 45°C"};
colormap_list_temp = {'b', [1 0.5 0], [1 0.5 0], [1 0.5 0], [1 0.5 0], 'r'};
LineStyleList_temp = {'-','--',':','-.','-','-'};

figure('Position', [100, 100, 1400, 900]);
% Tile index = (row-1)*cols + col for a 2x2 layout: col 1 = tiles 1 & 3, col 2 = tiles 2 & 4.
tDoDTemp = tiledlayout(2, 2, 'TileSpacing', 'compact', 'Padding', 'compact');
plotResistanceIncreaseTile(nexttile(tDoDTemp, 1), nexttile(tDoDTemp, 3), combined_df_cyclic, cell_list, ...
    cell_plot_list_dod, label_list_dod, colormap_list_dod, Marker_list_dod, 'Effect of Depth of Discharge (DoD)');
plotResistanceIncreaseTile(nexttile(tDoDTemp, 2), nexttile(tDoDTemp, 4), combined_df_cyclic, cell_list, ...
    cell_plot_list_temp, label_list_temp, colormap_list_temp, LineStyleList_temp, 'Effect of Temperature');

output_file = fullfile(pngsDir, 'CheckupResistanceVsTime_DoD_Temperature_.png');
exportgraphics(gcf, output_file, 'Resolution', 300);

%% Plot 4+6: C-rate effect at 0°C (col 1), 25°C (col 2), 45°C (col 3), combined 3x2 tiles.
fprintf('Generating Plot 4+6: C-rate effect at 0°C / 25°C / 45°C (combined)...\n');
cell_plot_list_0C = {"Cell_63", "Cell_60", "Cell_66", "Cell_68"};
label_list_0C = {"Cell_63 - C/4 - C/2 - 0°C", "Cell_60 - C/2 - C/2 - 0°C", "Cell_66 - 3C/4 - C/2 - 0°C", "Cell_68 - 1C - C/2 - 0°C"};
colormap_list_0C = {'b', 'r', 'g', [1 0.5 0]};
Marker_list_0C = repmat({'-'}, 1, length(cell_plot_list_0C));

cell_plot_list_25C = {"Cell_12", "Cell_23", "Cell_34", "Cell_35"};
label_list_25C = {"Cell_12 - C/2 - C/2", "Cell_23 - 1C - C/2", "Cell_34 - 3C/2 - C/2", "Cell_35 - 2C - C/2"};
% Reuse the project publication palette (R-017) - same highlighted cells as fig. 5's dedicated export below.
colormap_list_25C = {[12 195 82]./255, [1 17 181]./255, [255 0 0]./255, [255 0 255]./255};
Marker_list_25C = repmat({'-'}, 1, length(cell_plot_list_25C));

cell_plot_list_45C = {"Cell_29", "Cell_8", "Cell_9", "Cell_47"};
label_list_45C = {"Cell_29 - C/2 - C/2 - 45°C", "Cell_8 - C/2 - 1C - 45°C", "Cell_9 - 1C - C/2 - 45°C", "Cell_47 - 1C - 1C - 45°C"};
colormap_list_45C = {'b', 'r', 'g', [1 0.5 0]};
Marker_list_45C = repmat({'-'}, 1, length(cell_plot_list_45C));

figure('Position', [100, 100, 1600, 900]);
% Tile index = (row-1)*cols + col for a 2x3 layout: cols = 0°C/25°C/45°C, rows = Time/FEC.
tCrate = tiledlayout(2, 3, 'TileSpacing', 'compact', 'Padding', 'compact');
plotResistanceIncreaseTile(nexttile(tCrate, 1), nexttile(tCrate, 4), combined_df_cyclic, cell_list, ...
    cell_plot_list_0C, label_list_0C, colormap_list_0C, Marker_list_0C, 'Effect of C-rate at 0°C');
plotResistanceIncreaseTile(nexttile(tCrate, 2), nexttile(tCrate, 5), combined_df_cyclic, cell_list, ...
    cell_plot_list_25C, label_list_25C, colormap_list_25C, Marker_list_25C, 'Effect of C-rate at 25°C');
plotResistanceIncreaseTile(nexttile(tCrate, 3), nexttile(tCrate, 6), combined_df_cyclic, cell_list, ...
    cell_plot_list_45C, label_list_45C, colormap_list_45C, Marker_list_45C, 'Effect of C-rate at 45°C');

output_file = fullfile(pngsDir, 'CheckupResistanceVsTime_CrateComparison_.png');
exportgraphics(gcf, output_file, 'Resolution', 300);

%% Plot 5: Effect of C-rate at 25°C (dedicated journal publication export, fig. 5 - kept standalone).
fprintf('Generating Plot 5: Effect of C-rate at 25°C (publication export)...\n');
cell_plot_list = {"Cell_12", "Cell_23", "Cell_34", "Cell_35"};
label_list = {"Cell_12 - C/2 - C/2", "Cell_23 - 1C - C/2", "Cell_34 - 3C/2 - C/2", "Cell_35 - 2C - C/2"};
% Use the project publication palette (R-017) for the highlighted cyclic cells.
colormap_list = {[12 195 82]./255, [1 17 181]./255, [255 0 0]./255, [255 0 255]./255};
Marker_list = repmat({'-'}, 1, length(cell_plot_list));
output_file = fullfile(pngsDir, 'CheckupResistanceVsTime_VsCrate25DegC_.png');
output_file_pdf = fullfile(pngsDir, 'CheckupResistanceVsTime_VsCrate25DegC_.pdf');
% Plot 5: C-rate at 25°C with resistance in milliohms
resistanceScale_plot5 = 1000; % convert Ohm to mOhm
% latex interpreter; \vphantom{\tilde{C}} reserves the same height as the capacity
% figure's C-tilde label so both y-labels have an identical left margin / plot-box width.
resistanceLabel_plot5 = '$\vphantom{\tilde{C}}R_{RPT}\,[\mathrm{m}\Omega]$';
yLimits_plot5 = [1.10, 9.00];
plotResistanceIncreasePublicationFECOnly(combined_df_cyclic, cell_list, cell_plot_list, label_list, colormap_list, Marker_list, ...
    output_file, output_file_pdf, '', 'northeast', resistanceScale_plot5, resistanceLabel_plot5, yLimits_plot5);

%% Plot 7+8: Effect of Average SoC (col 1) and Effect of High Voltage (col 2), combined 2x2 tiles.
fprintf('Generating Plot 7+8: Effect of Average SoC + Effect of High Voltage (combined)...\n');
cell_plot_list_soc = {"Cell_40", "Cell_1", "Cell_3"};
label_list_soc = {"Cell_40 - 75% avg SoC - 50% DoD", "Cell_1 - 50% avg SoC - 50% DoD", "Cell_3 - 25% avg SoC - 50% DoD"};
colormap_list_soc = {'b', 'r', 'g'};
Marker_list_soc = repmat({'-'}, 1, length(cell_plot_list_soc));

cell_plot_list_highv = {"Cell_9", "Cell_5", "Cell_22"};
label_list_highv = {"Cell_9 - 2.75V-4.35V - CC - CC", "Cell_5 - 2.75V-4.45V - CC - CC", "Cell_22 - 2.75V-4.45V - CCCV - CC"};
colormap_list_highv = {'b', 'r', 'g'};
Marker_list_highv = repmat({'-'}, 1, length(cell_plot_list_highv));

figure('Position', [100, 100, 1400, 900]);
% Tile index = (row-1)*cols + col for a 2x2 layout: col 1 = tiles 1 & 3, col 2 = tiles 2 & 4.
tSoCHighV = tiledlayout(2, 2, 'TileSpacing', 'compact', 'Padding', 'compact');
plotResistanceIncreaseTile(nexttile(tSoCHighV, 1), nexttile(tSoCHighV, 3), combined_df_cyclic, cell_list, ...
    cell_plot_list_soc, label_list_soc, colormap_list_soc, Marker_list_soc, 'Effect of Average SoC');
plotResistanceIncreaseTile(nexttile(tSoCHighV, 2), nexttile(tSoCHighV, 4), combined_df_cyclic, cell_list, ...
    cell_plot_list_highv, label_list_highv, colormap_list_highv, Marker_list_highv, 'Effect of High Voltage');

output_file = fullfile(pngsDir, 'CheckupResistanceVsTime_AvgSoC_HighV_.png');
exportgraphics(gcf, output_file, 'Resolution', 300);

%% Plot 10+11: Stationary storage cycle (col 1) and Drive cycle (col 2), combined 2x2 tiles.
fprintf('Generating Plot 10+11: Stationary storage cycle + Drive cycle (combined)...\n');
cell_plot_list_stat = {"Cell_74", "Cell_46", "Cell_17", "Cell_53"};
label_list_stat = {"Cell_74 - 0 degrees", "Cell_46 - 25 degrees", "Cell_17 - 45 degrees", "Cell_53 - dynamic temperatures"};
colormap_list_stat = {'b', 'r', 'g', [0.5 0.5 0.5]};
Marker_list_stat = repmat({'-'}, 1, length(cell_plot_list_stat));

cell_plot_list_drive = {"Cell_72", "Cell_42", "Cell_25", "Cell_49"};
label_list_drive = {"Cell_72 - 0 degrees", "Cell_42 - 25 degrees", "Cell_14 - 45 degrees", "Cell_49 - dynamic temperatures"};
colormap_list_drive = {'b', 'r', 'g', [0.5 0.5 0.5]};
Marker_list_drive = repmat({'-'}, 1, length(cell_plot_list_drive));

figure('Position', [100, 100, 1400, 900]);
% Tile index = (row-1)*cols + col for a 2x2 layout: col 1 = tiles 1 & 3, col 2 = tiles 2 & 4.
tStatDrive = tiledlayout(2, 2, 'TileSpacing', 'compact', 'Padding', 'compact');
plotResistanceIncreaseTile(nexttile(tStatDrive, 1), nexttile(tStatDrive, 3), combined_df_cyclic, cell_list, ...
    cell_plot_list_stat, label_list_stat, colormap_list_stat, Marker_list_stat, 'Stationary storage cycle');
plotResistanceIncreaseTile(nexttile(tStatDrive, 2), nexttile(tStatDrive, 4), combined_df_cyclic, cell_list, ...
    cell_plot_list_drive, label_list_drive, colormap_list_drive, Marker_list_drive, 'Drive cycle');

output_file = fullfile(pngsDir, 'CheckupResistanceVsTime_StationaryStorage_DriveCycle_.png');
exportgraphics(gcf, output_file, 'Resolution', 300);

%% Plot 9: Calendar Ageing Effect (highlighted cells)
fprintf('Generating Plot 9: Calendar Ageing Effect (publication export)...\n');
% Keep the same 5 highlighted calendar cells as the paired capacity figure
% so subplot (a) and (b) are directly comparable and no un-labeled gray line remains.
cell_plot_list_calendar = {"Cell_57", "Cell_11", "Cell_45", "Cell_26", "Cell_28"};
avg_soc_calendar_cells = [1, 1, 0.1, 0.5, 1];
label_list_calendar = { ...
    sprintf("Cell_57 | 0°C | (Avg SoC = %.1f%%)", avg_soc_calendar_cells(1)*100), ...
    sprintf("Cell_11 | 25°C | (Avg SoC = %.1f%%)", avg_soc_calendar_cells(2)*100), ...
    sprintf("Cell_45 | 45°C | (Avg SoC = %.1f%%)", avg_soc_calendar_cells(3)*100), ...
    sprintf("Cell_26 | 45°C | (Avg SoC = %.1f%%)", avg_soc_calendar_cells(4)*100), ...
    sprintf("Cell_28 | 45°C | (Avg SoC = %.1f%%)", avg_soc_calendar_cells(5)*100) ...
};

% Use the project publication palette (R-017) for calendar figure highlights.
green_pub = [12 195 82] ./ 255;
darkblue_pub = [1 17 181] ./ 255;
red_pub = [255 0 0] ./ 255;
magenta_pub = [255 0 255] ./ 255;
black_pub = [0 0 0];
colormap_list_calendar = {green_pub, darkblue_pub, red_pub, magenta_pub, black_pub};
Marker_list_calendar = repmat({'-'}, 1, length(cell_plot_list_calendar));
output_file_calendar = fullfile(pngsDir, 'CheckupResistanceVsTime_CalendarAgeingEffect_.png');
output_file_calendar_pdf = fullfile(pngsDir, 'CheckupResistanceVsTime_CalendarAgeingEffect_.pdf');
calendar_label = '';
resistanceScale = 1000; % convert Ohm to mOhm for publication display
% latex interpreter; \vphantom{\tilde{C}} reserves the same height as the capacity
% figure's C-tilde label so both y-labels have an identical left margin / plot-box width.
resistanceLabel = '$\vphantom{\tilde{C}}R_{RPT}\,[\mathrm{m}\Omega]$';
calendarYLim = [1.10 9.00]*1e-3*resistanceScale;
calendarXLim = [0, 400]; % consistent x-range for publication figures
calendarPlainTitle = true; % remove '(vs Time)' from this publication figure title
plotResistanceIncreasePublication(combined_df_calendar, cell_list_calendar, cell_plot_list_calendar, label_list_calendar, colormap_list_calendar, Marker_list_calendar, ...
    output_file_calendar, output_file_calendar_pdf, calendar_label, false, 'northwest', resistanceScale, resistanceLabel, calendarYLim, calendarPlainTitle, calendarXLim);

fprintf('--- PlotResistanceIncrease_Detailed: figure generation complete ---\n');
disp('All resistance plots generated successfully!');

%% Helper functions
function combined_df = renameColumnIfExists(combined_df, old_name, new_name)
    if any(strcmp(combined_df.Properties.VariableNames, old_name))
        combined_df.Properties.VariableNames{old_name} = new_name;
    end
end

function plotResistanceIncrease(combined_df, cell_list, cell_plot_list, label_list, colormap_list, Marker_list, filename, plot_title)
    figure('Position', [100, 100, 1000, 900]);

    % --- Subplot 1: Time vs Resistance ---
    subplot(2,1,1);
    hold on;
    for i = 1:length(cell_list)
        cell_name = cell_list{i};
        mask = strcmp(combined_df.("cell_number"), cell_name);
        current_cell = combined_df(mask, :);
        if isempty(current_cell)
            continue;
        end
        time_days = days(current_cell.("Timestamp") - current_cell.("Timestamp")(1));
        plot(time_days, current_cell.("Resistance [Ohm]"), 'LineWidth', 2, 'Color', [0.5 0.5 0.5 0.2], 'HandleVisibility', 'off');
    end

    legend_handles = [];
    legend_labels = {};
    for idx = 1:length(cell_plot_list)
        cell_name = cell_plot_list{idx};
        mask = strcmp(combined_df.("cell_number"), cell_name);
        current_cell = combined_df(mask, :);
        if isempty(current_cell)
            warning('Cell %s not found in data', cell_name);
            continue;
        end
        time_days = days(current_cell.("Timestamp") - current_cell.("Timestamp")(1));
        h = plot(time_days, current_cell.("Resistance [Ohm]"), '-o', 'LineWidth', 2, 'Color', colormap_list{idx}, ...
            'MarkerFaceColor', colormap_list{idx}, 'MarkerSize', 4, 'LineStyle', Marker_list{idx});
        legend_handles = [legend_handles, h];
        legend_labels{end+1} = label_list{idx};
    end

    grid on;
    set(gca, 'GridLineStyle', '--', 'GridAlpha', 0.6);
    xlabel('Time [days]', 'FontSize', 11);
    ylabel('Checkup Resistance [\Omega]', 'FontSize', 11);
    if ~isempty(plot_title)
        title([plot_title ' (vs Time)'], 'FontSize', 11);
    end
    if ~isempty(legend_handles)
        legend(legend_handles, legend_labels, 'FontSize', 11, 'Location', 'best', 'Interpreter', 'none');
    end
    hold off;

    % --- Subplot 2: FEC vs Resistance ---
    subplot(2,1,2);
    hold on;
    maxFEC_highlight = 0;

    for i = 1:length(cell_list)
        cell_name = cell_list{i};
        mask = strcmp(combined_df.("cell_number"), cell_name);
        current_cell = combined_df(mask, :);
        if isempty(current_cell)
            continue;
        end
        plot(current_cell.CheckupResistanceFEC, current_cell.("Resistance [Ohm]"), 'LineWidth', 2, 'Color', [0.5 0.5 0.5 0.2], 'HandleVisibility', 'off');
    end

    legend_handles = [];
    legend_labels = {};
    for idx = 1:length(cell_plot_list)
        cell_name = cell_plot_list{idx};
        mask = strcmp(combined_df.("cell_number"), cell_name);
        current_cell = combined_df(mask, :);
        if isempty(current_cell)
            continue;
        end
        h = plot(current_cell.CheckupResistanceFEC, current_cell.("Resistance [Ohm]"), '-o', 'LineWidth', 2, 'Color', colormap_list{idx}, ...
            'MarkerFaceColor', colormap_list{idx}, 'MarkerSize', 4, 'LineStyle', Marker_list{idx});
        legend_handles = [legend_handles, h];
        legend_labels{end+1} = label_list{idx};
        if ~isempty(current_cell.CheckupResistanceFEC)
            maxFEC_highlight = max([maxFEC_highlight; max(current_cell.CheckupResistanceFEC)]);
        end
    end

    grid on;
    set(gca, 'GridLineStyle', '--', 'GridAlpha', 0.6);
    xlabel('Full Equivalent Cycles (FEC)', 'FontSize', 11);
    ylabel('Checkup Resistance [\Omega]', 'FontSize', 11);
    if ~isempty(plot_title)
        title([plot_title ' (vs FEC)'], 'FontSize', 11);
    end
    if maxFEC_highlight > 0
        xlim([0, maxFEC_highlight]);
    end
    if ~isempty(legend_handles)
        legend(legend_handles, legend_labels, 'FontSize', 11, 'Location', 'best', 'Interpreter', 'none');
    end
    hold off;

    exportgraphics(gcf, filename, 'Resolution', 300);
end

function plotResistanceIncreasePublication(combined_df, cell_list, cell_plot_list, label_list, colormap_list, Marker_list, pngFilename, pdfFilename, plot_title, includeFEC, legendLocation, resistanceScale, resistanceLabel, yLimits, usePlainTopTitle, xLimits)
    % PLOTRESISTANCEINCREASEPUBLICATION Creates publication-ready resistance plots.

    if nargin < 11 || isempty(legendLocation)
        legendLocation = 'southwest';
    end
    if nargin < 12 || isempty(resistanceScale)
        resistanceScale = 1;
    end
    if nargin < 13 || isempty(resistanceLabel)
        resistanceLabel = 'Checkup Resistance [\Omega]';
    end
    if nargin < 14
        yLimits = [];
    end
    if nargin < 15 || isempty(usePlainTopTitle)
        usePlainTopTitle = false;
    end
    % Set default x-limits if not provided
    if nargin < 16 || isempty(xLimits)
        xLimits = [];
    end

    nHighlight = min([numel(cell_plot_list), numel(label_list), numel(colormap_list), numel(Marker_list)]);
    if nHighlight < numel(cell_plot_list)
        warning('Publication plot: list lengths differ, using first %d highlighted cells.', nHighlight);
    end

    % Axis labels, tick labels and legend all at the caption size (R-021).
    pubFontSize = 8;
    % R-021 documented exception for CheckupResistanceVsTime_CalendarAgeingEffect_.pdf
    % (the only figure this function produces): legend 1 pt smaller to avoid overlap.
    pubLegendFontSize = pubFontSize - 1;
    if includeFEC
        figWidth = 7.90;
        figHeight = 8.8;
    else
        % Requested compact size to match the paired capacity subfigure (fig. 11).
        figWidth = 8.0962;
        figHeight = 3.5190;
    end
    figPub = figure('Color', 'w', 'Units', 'centimeters', 'Position', [2, 2, figWidth, figHeight]);
    figPub.PaperPositionMode = 'auto';

    if includeFEC
        t = tiledlayout(figPub, 2, 1, 'TileSpacing', 'compact', 'Padding', 'compact');
    else
        t = tiledlayout(figPub, 1, 1, 'TileSpacing', 'compact', 'Padding', 'compact');
    end

    axTop = nexttile(t);
    hold(axTop, 'on');
    for i = 1:length(cell_list)
        cell_name = cell_list{i};
        mask = strcmp(combined_df.("cell_number"), cell_name);
        current_cell = combined_df(mask, :);
        if isempty(current_cell)
            continue;
        end
        time_days = days(current_cell.("Timestamp") - current_cell.("Timestamp")(1));
        % Auxiliary/background trace (R-017 item 4): 0.8 pt.
        plot(axTop, time_days, current_cell.("Resistance [Ohm]") * resistanceScale, 'LineWidth', 1.0, 'Color', [0.75 0.75 0.75], 'HandleVisibility', 'off');
    end

    legend_handles = [];
    legend_labels = {};
    for idx = 1:nHighlight
        cell_name = cell_plot_list{idx};
        mask = strcmp(combined_df.("cell_number"), cell_name);
        current_cell = combined_df(mask, :);
        if isempty(current_cell)
            continue;
        end
        time_days = days(current_cell.("Timestamp") - current_cell.("Timestamp")(1));
        % Primary data trace, non current/voltage (R-017 item 4): 1.0 pt.
        h = plot(axTop, time_days, current_cell.("Resistance [Ohm]") * resistanceScale, 'LineWidth', 1.0, 'Color', colormap_list{idx}, 'LineStyle', Marker_list{idx});
        legend_handles = [legend_handles, h]; %#ok<AGROW>
        legend_labels{end+1} = label_list{idx}; %#ok<AGROW>
    end

    grid(axTop, 'on'); box(axTop, 'on');
    if ~isempty(xLimits)
        xlim(axTop, xLimits);
        xticks(axTop, unique(sort([xticks(axTop), xLimits(1), xLimits(2)])));
    end
    if ~isempty(plot_title)
        if usePlainTopTitle
            title(axTop, plot_title, 'FontName', 'Times New Roman', 'FontSize', pubFontSize);
        else
            title(axTop, [plot_title ' (vs Time)'], 'FontName', 'Times New Roman', 'FontSize', pubFontSize);
        end
    end
    xlabel(axTop, 'Time [days]', 'FontName', 'Times New Roman', 'FontSize', pubFontSize);
    ylabel(axTop, resistanceLabel, 'FontName', 'Times New Roman', 'FontSize', pubFontSize, 'Interpreter', 'latex');
    if ~isempty(yLimits)
        ylim(axTop, yLimits);
        yticks(axTop, unique(sort([yticks(axTop), yLimits(1), yLimits(2)])));
        % Ensure clean formatting for publication-quality appearance
        set(axTop, 'YMinorTick', 'off');
    end
    axTop.YTickLabel = arrayfun(@(v) sprintf('%.2f', v), axTop.YTick, 'UniformOutput', false);
    set(axTop, 'FontName', 'Times New Roman', 'FontSize', pubFontSize, 'LineWidth', 0.8);
    axTop.LabelFontSizeMultiplier = 1.0;
    axTop.TitleFontSizeMultiplier = 1.0;
    if ~isempty(legend_handles)
        lgdTop = legend(axTop, legend_handles, legend_labels, 'Interpreter', 'none', 'Location', legendLocation, 'Box', 'off', ...
            'FontName', 'Times New Roman', 'FontSize', pubLegendFontSize);
        % R-021 exception: nudge the legend up so it clears the data it overlapped.
        lgdTop.Units = 'normalized';
        lgdTop.Position(2) = lgdTop.Position(2) + 0.06;
    end

    if includeFEC
        axBottom = nexttile(t);
        hold(axBottom, 'on');

        maxFEC = 0;
        for i = 1:length(cell_list)
            cell_name = cell_list{i};
            mask = strcmp(combined_df.("cell_number"), cell_name);
            current_cell = combined_df(mask, :);
            if isempty(current_cell)
                continue;
            end
            % Auxiliary/background trace (R-017 item 4): 0.8 pt.
            plot(axBottom, current_cell.CheckupResistanceFEC, current_cell.("Resistance [Ohm]") * resistanceScale, 'LineWidth', 1.0, 'Color', [0.75 0.75 0.75], 'HandleVisibility', 'off');
            if ~isempty(current_cell.CheckupResistanceFEC)
                maxFEC = max(maxFEC, max(current_cell.CheckupResistanceFEC));
            end
        end

        legend_handles = [];
        legend_labels = {};
        maxFEC_highlight = 0;
        for idx = 1:nHighlight
            cell_name = cell_plot_list{idx};
            mask = strcmp(combined_df.("cell_number"), cell_name);
            current_cell = combined_df(mask, :);
            if isempty(current_cell)
                continue;
            end
            % Primary data trace, non current/voltage (R-017 item 4): 1.0 pt.
            h = plot(axBottom, current_cell.CheckupResistanceFEC, current_cell.("Resistance [Ohm]") * resistanceScale, 'LineWidth', 1.0, 'Color', colormap_list{idx}, 'LineStyle', Marker_list{idx});
            legend_handles = [legend_handles, h]; %#ok<AGROW>
            legend_labels{end+1} = label_list{idx}; %#ok<AGROW>
            if ~isempty(current_cell.CheckupResistanceFEC)
                maxFEC_highlight = max(maxFEC_highlight, max(current_cell.CheckupResistanceFEC));
            end
        end

        if maxFEC_highlight <= 0
            maxFEC_highlight = maxFEC;
        end

        grid(axBottom, 'on'); box(axBottom, 'on');
        if maxFEC_highlight > 0
            xlim(axBottom, [0, maxFEC_highlight]);
        end
        if ~isempty(plot_title)
            title(axBottom, [plot_title ' (vs FEC)'], 'FontName', 'Times New Roman', 'FontSize', pubFontSize);
        end
        xlabel(axBottom, 'Full Equivalent Cycles [cycles]', 'FontName', 'Times New Roman', 'FontSize', pubFontSize);
        ylabel(axBottom, resistanceLabel, 'FontName', 'Times New Roman', 'FontSize', pubFontSize, 'Interpreter', 'latex');
        if ~isempty(yLimits)
            ylim(axBottom, yLimits);
            yticks(axBottom, unique(sort([yticks(axBottom), yLimits(1), yLimits(2)])));
        end
        axBottom.YTickLabel = arrayfun(@(v) sprintf('%.2f', v), axBottom.YTick, 'UniformOutput', false);
        set(axBottom, 'FontName', 'Times New Roman', 'FontSize', pubFontSize, 'LineWidth', 0.8);
        axBottom.LabelFontSizeMultiplier = 1.0;
        axBottom.TitleFontSizeMultiplier = 1.0;
        if ~isempty(legend_handles)
            legend(axBottom, legend_handles, legend_labels, 'Interpreter', 'none', 'Location', legendLocation, 'Box', 'off', ...
                'FontName', 'Times New Roman', 'FontSize', pubLegendFontSize);
        end
    end

    exportgraphics(figPub, pngFilename, 'Resolution', 300);
    exportgraphics(figPub, pdfFilename, 'ContentType', 'vector');
    fprintf('Publication PDF saved: %s\n', pdfFilename);

    % [pdfFolder, pdfBase, ~] = fileparts(pdfFilename);
    % figFilename = fullfile(pdfFolder, [pdfBase, '.fig']);
    % savefig(figPub, figFilename);
    % fprintf('Publication FIG saved: %s\n', figFilename);
end

function plotResistanceIncreasePublicationFECOnly(combined_df, cell_list, cell_plot_list, label_list, colormap_list, Marker_list, pngFilename, pdfFilename, plot_title, legendLocation, resistanceScale, resistanceLabel, yLimits)
    % PLOTRESISTANCEINCREASEPUBLICATIONFECONLY Creates a publication figure with only the FEC subplot.

    if nargin < 10 || isempty(legendLocation)
        legendLocation = 'southwest';
    end
    % Set default resistance scaling (no scaling) if not provided
    if nargin < 11 || isempty(resistanceScale)
        resistanceScale = 1;
    end
    if nargin < 12 || isempty(resistanceLabel)
        resistanceLabel = 'Checkup Resistance [\Omega]';
    end
    if nargin < 13 || isempty(yLimits)
        yLimits = [];
    end

    nHighlight = min([numel(cell_plot_list), numel(label_list), numel(colormap_list), numel(Marker_list)]);
    if nHighlight < numel(cell_plot_list)
        warning('Publication plot (FEC only): list lengths differ, using first %d highlighted cells.', nHighlight);
    end

    % Axis labels, tick labels and legend all at the caption size (R-021).
    pubFontSize = 8;
    pubLegendFontSize = pubFontSize;   % R-021: legend text = axis-label/caption size (no reduction)
    % Requested compact size to match the paired capacity subfigure (fig. 5).
    figPub = figure('Color', 'w', 'Units', 'centimeters', 'Position', [2, 2, 8.0962, 3.5190]);
    figPub.PaperPositionMode = 'auto';
    t = tiledlayout(figPub, 1, 1, 'TileSpacing', 'compact', 'Padding', 'compact');
    axBottom = nexttile(t);
    hold(axBottom, 'on');

    maxFEC = 0;
    for i = 1:length(cell_list)
        cell_name = cell_list{i};
        mask = strcmp(combined_df.("cell_number"), cell_name);
        current_cell = combined_df(mask, :);
        if isempty(current_cell)
            continue;
        end
        % Auxiliary/background trace (R-017 item 4): 0.8 pt.
        plot(axBottom, current_cell.CheckupResistanceFEC, current_cell.("Resistance [Ohm]") * resistanceScale, 'LineWidth', 1.0, 'Color', [0.75 0.75 0.75], 'HandleVisibility', 'off');
        if ~isempty(current_cell.CheckupResistanceFEC)
            maxFEC = max(maxFEC, max(current_cell.CheckupResistanceFEC));
        end
    end

    legend_handles = [];
    legend_labels = {};
    maxFEC_highlight = 0;
    for idx = 1:nHighlight
        cell_name = cell_plot_list{idx};
        mask = strcmp(combined_df.("cell_number"), cell_name);
        current_cell = combined_df(mask, :);
        if isempty(current_cell)
            continue;
        end
        % Primary data trace, non current/voltage (R-017 item 4): 1.0 pt.
        h = plot(axBottom, current_cell.CheckupResistanceFEC, current_cell.("Resistance [Ohm]") * resistanceScale, 'LineWidth', 1.0, 'Color', colormap_list{idx}, 'LineStyle', Marker_list{idx});
        legend_handles = [legend_handles, h]; %#ok<AGROW>
        legend_labels{end+1} = label_list{idx}; %#ok<AGROW>
        if ~isempty(current_cell.CheckupResistanceFEC)
            maxFEC_highlight = max(maxFEC_highlight, max(current_cell.CheckupResistanceFEC));
        end
    end

    if maxFEC_highlight <= 0
        maxFEC_highlight = maxFEC;
    end

    grid(axBottom, 'on'); box(axBottom, 'on');
    if maxFEC_highlight > 0
        xlim(axBottom, [0, maxFEC_highlight]);
    end
    % Ensure clean axis formatting
    set(axBottom, 'YMinorTick', 'off');
    if ~isempty(yLimits)
        ylim(axBottom, yLimits);
        yticks(axBottom, unique(sort([yticks(axBottom), yLimits(1), yLimits(2)])));
    end
    axBottom.YTickLabel = arrayfun(@(v) sprintf('%.2f', v), axBottom.YTick, 'UniformOutput', false);
    if ~isempty(plot_title)
        title(axBottom, plot_title, 'FontName', 'Times New Roman', 'FontSize', pubFontSize);
    end
    xlabel(axBottom, 'Full Equivalent Cycles [cycles]', 'FontName', 'Times New Roman', 'FontSize', pubFontSize);
    ylabel(axBottom, resistanceLabel, 'FontName', 'Times New Roman', 'FontSize', pubFontSize, 'Interpreter', 'latex');
    set(axBottom, 'FontName', 'Times New Roman', 'FontSize', pubFontSize, 'LineWidth', 0.8);
    axBottom.LabelFontSizeMultiplier = 1.0;
    axBottom.TitleFontSizeMultiplier = 1.0;
    if ~isempty(legend_handles)
        legend(axBottom, legend_handles, legend_labels, 'Interpreter', 'none', 'Location', legendLocation, 'Box', 'off', ...
            'FontName', 'Times New Roman', 'FontSize', pubLegendFontSize);
    end

    exportgraphics(figPub, pngFilename, 'Resolution', 300);
    exportgraphics(figPub, pdfFilename, 'ContentType', 'vector');
    fprintf('Publication PDF saved: %s\n', pdfFilename);

    % [pdfFolder, pdfBase, ~] = fileparts(pdfFilename);
    % figFilename = fullfile(pdfFolder, [pdfBase, '.fig']);
    % savefig(figPub, figFilename);
    % fprintf('Publication FIG saved: %s\n', figFilename);
end
